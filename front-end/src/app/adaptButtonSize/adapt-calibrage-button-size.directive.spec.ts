import { AdaptCalibrageButtonSizeDirective } from './adapt-calibrage-button-size.directive';

describe('AdaptCalibrageButtonSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AdaptCalibrageButtonSizeDirective();
    expect(directive).toBeTruthy();
  });
});
